Test (`Multiplying different integers that gives a correct result`, () => {
    expect(multiplication (2,3)) .toBe (6);
    expect (multiplication (0,10)) .toBe (0);
})

// Functional Test
test('Ensure concatenation of odd numbers works correctly in the application', async () => {}
);

